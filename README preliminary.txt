Ensure that pandas package is installed on your python IDE when running the python scripts.

Also ensure that the the CCF zip folder is in THIS directory, so that the BAT script can initially run the yarn command.